(function() {
  var $modal = $('#modal-window')
  $modal.find(".modal-content").html("<%= j (render partial) %>");
  $modal.removeClass('rights');
  $modal.addClass("<%= options[:class] %>")
  $modal.modal();
})()
